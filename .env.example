# UID/GID хост-пользователя — docker-compose передаёт их в Dockerfile как build args.
# php-fpm внутри контейнера будет работать от пользователя с этими ID,
# поэтому файлы storage/ и bootstrap/cache/ создаются с нужными правами.
#
# Получить текущие значения: id -u  /  id -g
UID=1000
GID=1000
